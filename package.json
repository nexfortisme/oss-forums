{
  "name": "oss-forums",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bunx --parallel bun:run dev:web dev:api",
    "dev:web": "bun --cwd apps/web dev",
    "dev:api": "bun --cwd apps/api dev",
    "build:web": "bun --cwd apps/web build",
    "build:api": "bun --cwd apps/api build"
  },
  "devDependencies": {
  }
}
